// This file previously contained demo data
// Now it's cleaned to work with real database data only

export const initializeDemoData = () => {
  // This function is now empty as we don't want any dummy data
  console.log('Demo data initialization skipped - using real database');
};
